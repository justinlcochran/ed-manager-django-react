Flexible mode or Strict mode (can be set by admin??) ((Starting with strict mode only))

Enrollment Create Workflow:
    Content Dropdown
    StandardSet Dropdown

    Choose Standards Set (Foreign Key Standard Magnet) Grade Levels? (Autochoose if length 1, filter standard sets by content and grade
    Write Title
    Invite Code? or Filter Inv List