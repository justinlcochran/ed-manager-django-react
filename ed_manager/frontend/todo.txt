Flexible mode or Strict mode (can be set by admin??) ((Starting with strict mode only))

When all knowRequired in knowSatisfied, contextual SUBMIT ASSESSMENT button appears ({knowShowRequired == knowShowSatisfied && Submit Button})

Actual submit, backend receives objects as array of question objects containing answer objects. Unpack each, instantiate in backend, then display "What's next?" links
